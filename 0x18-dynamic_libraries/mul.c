#include <stdlib.h>
#include <stdio.h>

/**
 *mul - multiplication of  two integers
 * @a: first integer
 * @b: second integer
 * return: multiply end result
 */
int mul(int a, int b)
{
	return (a * b);
}
